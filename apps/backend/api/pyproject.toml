[tool.poetry]
name = "api"
version = "0.1.0"
description = "DreamManor - FastApi and Quasar"
authors = ["kochenov <kochenof@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.110.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
